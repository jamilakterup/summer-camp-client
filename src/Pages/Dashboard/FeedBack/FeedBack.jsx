import {Helmet} from "react-helmet";
import SectionTitle from "../../../components/SectionTitle/SectionTitle";

const FeedBack = () => {
    return (
        <div>
            <Helmet title="SM Academy/dashboard/feedback" />
            <SectionTitle heading='Feedback' />
        </div>
    );
};

export default FeedBack;